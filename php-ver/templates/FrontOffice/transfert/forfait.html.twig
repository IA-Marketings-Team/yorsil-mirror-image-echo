{% if forfaits is defined %}

{% endif %}

{# <div class="ibox animated fadeInRight">
    <div class="ibox-title">
        <h5>Sélectionnez le montant de la recharge</h5>
    </div>
    <div class="ibox-content">
        <form>
            <div class="row">
            {% if forfaits.fixedAmounts | length %}
                {% for amount in forfaits.fixedAmounts %}
                    <div class="col-md-4">
                        <div class="ibox">
                            <div class="ibox-content product-box">
                                <div class="text-center">
                                    <h4>{{amount}} {{forfaits.senderCurrencyCode}}</h4>
                                    <h4>
                                        <small class="text-muted">
                                            <i class="fa fa-exchange-alt"></i>
                                        </small> {{ amount|length }} €
                                    </h4>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            {% else %}
                <p>Montant à transmetre doit être compris entre <strong>{{forfaits.minAmount}}€</strong> et <strong>{{forfaits.maxAmount}}€</strong>  </p>
                <div class="form-group row">
                    <div class="col-lg-8">
                        <div class="input-group m-b">
                            <input type="number" class="form-control" placeholder="Montant à transmetre">
                            <div class="input-group-append">
                                <span class="input-group-addon">€</span>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-12">
                        <button class="btn btn-sm btn-white" type="submit">Confirmer</button>
                    </div>
                    
                </div>
            {% endif %}
            </div>
        </form>
    </div>
</div> #}


{# <div class="col-lg-5">
            <div class="ibox ">
                <div class="ibox-title">
                    <h5>Sélectionnez le montant de la recharge</h5>
                </div>
                <div class="ibox-content">
                    <form>
                        <p>Sign in today for more expirience.</p>
                        <div class="form-group row"><label class="col-lg-2 col-form-label">Email</label>

                            <div class="col-lg-10">
                                <input type="email" placeholder="Email" class="form-control">
                                <span class="form-text m-b-none">Example block-level help text here.</span>
                            </div>
                        </div>
                        <div class="form-group row"><label class="col-lg-2 col-form-label">Password</label>
                            <div class="col-lg-10">
                                <input type="password" placeholder="Password" class="form-control">
                            </div>
                        </div>
                        <div class="form-group row">
                            <div class="col-lg-offset-2 col-lg-10">
                                <button class="btn btn-sm btn-primary float-right m-t-n-xs" type="submit">Sign in</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div> #}


{#  #}
