{% extends "base_error.html.twig" %}

{% block title %} Page non trouvée{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        #error-500 {
            position: relative;
            width: 100%;
            min-height: 100vh;
            background: linear-gradient(0deg, black, #565656, black);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 2rem;
            flex-direction: column;
            padding: 35px;
        }
        #error-500 .img-erreur-500{
            max-width: 100%;
            height: 60vh;
            object-fit: contain;
            box-shadow: 0 0 10px #e84e0f, 0 0 20px #e84e0f;
            border-radius: 10px;
        }
        #error-500 .to-home{
            
        }
        #error-500 p{
            color: #fff;
            font-size: 25px;
            margin: 0;
            padding: 0;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.8), 
                        0 0 20px rgba(255, 255, 255, 0.5);
            text-align: center;
        }
        @media screen and (width < 600px){
            #error-500 .img-erreur-500{
                height: auto;
            }
        }
    </style>
{% endblock %}

{% block body %}

    <div id="error-500">
        <p>Le serveur a rencontré quelque chose d'inattendu !</p>
        <img src="{{ asset('modernize/images/error/erreur500.png') }}" class="img-erreur-500" alt="erreur500">
        <p>qui ne lui a pas permis de terminer la demande!</p>
        <a href="{{ path('home_back')}}" class="to-home btn btn-outline-danger btn-sm m-t fs-5">Tableau de bord</a>
    </div>

{% endblock %}